<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_report_expense_voucher" model="ir.actions.report">
        <field name="name">Expense Voucher</field>
        <field name="model">mcit.expense</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">mcit_expense.report_expense_voucher</field>
        <field name="report_file">mcit_expense.report_expense_voucher</field>
        <field name="print_report_name">'Expense Voucher - %s' % (object.name)</field>
        <field name="binding_model_id" ref="model_mcit_expense"/>
        <field name="binding_type">report</field>
    </record>

    <template id="report_expense_voucher">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="o">
                <t t-call="web.external_layout">
                    <t t-call="mcit_base.report_voucher_document"/>
                </t>
            </t>
        </t>
    </template>
</odoo>
